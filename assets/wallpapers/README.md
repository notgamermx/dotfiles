# Wallpapers Directory

Place your `.mp4` video files in this folder.

By default, `scripts/set-bg.sh` looks for:
```text
assets/wallpapers/default.mp4
```

You can replace `default.mp4` with any high-definition video loop of your choice!
